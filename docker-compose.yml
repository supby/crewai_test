services:
  crewai:
    build:
      context: .
      dockerfile: Dockerfile
    image: crewai-jira-dev-flow
    container_name: crewai-jira-dev-flow
    env_file:
      - .env
    volumes:
      - ./output:/app/output
      - workspace:/workspace
    restart: "no"

volumes:
  workspace:
